/**
 * Design System Tokens
 * Central export for all design tokens
 */

export { colors, default as colorTokens } from './colors';
export { typography, default as typographyTokens } from './typography';
export { spacing, default as spacingTokens } from './spacing';

export const tokens = {
  colors,
  typography,
  spacing,
};

export default tokens;
