name: Test Suites

on:
  pull_request:
    branches: [ main, develop ]
  push:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  # Job 1: Run all test suites in parallel using matrix strategy
  test-suites:
    name: ${{ matrix.suite }} Tests
    runs-on: ubuntu-latest
    
    strategy:
      # Don't cancel other matrix jobs if one fails
      fail-fast: false
      matrix:
        suite:
          - unit
          - base-operations
          - database
          - integration
          - e2e
          - services
          - components
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run ${{ matrix.suite }} test suite
        run: npm run test:suite:${{ matrix.suite }}
        continue-on-error: false
      
      - name: Generate coverage for ${{ matrix.suite }}
        if: success()
        run: npm run test:suite:${{ matrix.suite }}:coverage
        continue-on-error: true
      
      - name: Upload coverage artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.suite }}
          path: coverage/${{ matrix.suite }}/
          retention-days: 7
      
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ matrix.suite }}
          path: |
            test-results/
            *.log
          retention-days: 7
          if-no-files-found: ignore

  # Job 2: Aggregate results and determine overall pass/fail
  test-results:
    name: Test Results Summary
    runs-on: ubuntu-latest
    needs: test-suites
    if: always()
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Check test suite results
        id: check-results
        run: |
          # Check if all test suite jobs passed
          if [ "${{ needs.test-suites.result }}" == "success" ]; then
            echo "✅ All test suites passed!"
            echo "status=success" >> $GITHUB_OUTPUT
            echo "message=All 7 test suites passed successfully" >> $GITHUB_OUTPUT
            exit 0
          else
            echo "❌ Some test suites failed!"
            echo "status=failure" >> $GITHUB_OUTPUT
            echo "message=One or more test suites failed" >> $GITHUB_OUTPUT
            exit 1
          fi
      
      - name: Download all coverage artifacts
        if: always()
        uses: actions/download-artifact@v4
        with:
          pattern: coverage-*
          path: coverage-reports/
      
      - name: Create summary report
        if: always()
        run: |
          echo "# Test Suite Results 📊" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Overall Status: ${{ steps.check-results.outputs.status == 'success' && '✅ PASSED' || '❌ FAILED' }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Suite Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Suite | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Unit | ${{ needs.test-suites.result == 'success' && '✅' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Base Operations | ${{ needs.test-suites.result == 'success' && '✅' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Database | ${{ needs.test-suites.result == 'success' && '✅' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Integration | ${{ needs.test-suites.result == 'success' && '✅' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| E2E | ${{ needs.test-suites.result == 'success' && '✅' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Services | ${{ needs.test-suites.result == 'success' && '✅' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Components | ${{ needs.test-suites.result == 'success' && '✅' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Coverage Reports" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Coverage reports are available as artifacts for each suite." >> $GITHUB_STEP_SUMMARY
      
      - name: Comment PR with results
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const status = '${{ steps.check-results.outputs.status }}';
            const message = '${{ steps.check-results.outputs.message }}';
            const icon = status === 'success' ? '✅' : '❌';
            
            const comment = `## ${icon} Test Suite Results
            
            **Status:** ${message}
            
            ### Suite Breakdown
            
            | Suite | Status |
            |-------|--------|
            | Unit | ${{ needs.test-suites.result == 'success' ? '✅ Passed' : '❌ Failed' }} |
            | Base Operations | ${{ needs.test-suites.result == 'success' ? '✅ Passed' : '❌ Failed' }} |
            | Database | ${{ needs.test-suites.result == 'success' ? '✅ Passed' : '❌ Failed' }} |
            | Integration | ${{ needs.test-suites.result == 'success' ? '✅ Passed' : '❌ Failed' }} |
            | E2E | ${{ needs.test-suites.result == 'success' ? '✅ Passed' : '❌ Failed' }} |
            | Services | ${{ needs.test-suites.result == 'success' ? '✅ Passed' : '❌ Failed' }} |
            | Components | ${{ needs.test-suites.result == 'success' ? '✅ Passed' : '❌ Failed' }} |
            
            Coverage reports are available in the workflow artifacts.
            `;
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });

  # Job 3: Set final status check
  final-status:
    name: Final Status Check
    runs-on: ubuntu-latest
    needs: [test-suites, test-results]
    if: always()
    
    steps:
      - name: Determine final status
        run: |
          if [ "${{ needs.test-suites.result }}" == "success" ]; then
            echo "✅ All test suites passed - PR can be merged"
            exit 0
          else
            echo "❌ Test suites failed - PR cannot be merged"
            exit 1
          fi
