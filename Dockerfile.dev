FROM node:20-alpine

WORKDIR /app

# Install dependencies first (cached layer)
COPY package*.json ./
RUN npm ci

# Source is mounted as a volume at runtime — don't copy it here
EXPOSE 3000

CMD ["npm", "run", "dev"]
